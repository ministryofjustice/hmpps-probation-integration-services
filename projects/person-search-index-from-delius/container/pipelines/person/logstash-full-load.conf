input {
    jdbc {
        id => "load-all-records"
        jdbc_driver_library => "/etc/logstash/ojdbc11.jar"
        jdbc_driver_class => "Java::oracle.jdbc.driver.OracleDriver"
        jdbc_connection_string => "${JDBC_CONNECTION_STRING}"
        jdbc_user => "${JDBC_USER}"
        jdbc_password => "${JDBC_PASSWORD}"
        jdbc_validate_connection => true
        lowercase_column_names => false
        statement_filepath => "/pipelines/person/statement.sql"
        parameters => {
            offender_id => 0
            batch_size => 100000
        }
        use_column_value => true
        tracking_column => "sql_next_value"
        tracking_column_type => "numeric"
        schedule => "* * * * *"
    }
}

filter {
    json {
        id => "parse-json"
        source => "json"
    }
    mutate {
        id => "remove-unused-fields"
        remove_field => [ "json", "next_sql_value" ]
    }
}

output {
    if [tags] and [tags][0] {
        sqs {
            id => "send-to-sqs-dlq"
            queue => "${PERSON_SQS_DLQ_NAME}"
            access_key_id => "${PERSON_SQS_DLQ_ACCESS_KEY_ID}"
            secret_access_key => "${PERSON_SQS_DLQ_SECRET_ACCESS_KEY}"
            endpoint => "${SQS_ENDPOINT:https://sqs.eu-west-2.amazonaws.com}"
            region => "eu-west-2"
        }
    } else {
        opensearch {
            id => "index-into-standby"
            hosts => ["${SEARCH_INDEX_HOST}"]
            index => "${PERSON_INDEX_PREFIX}-standby"
            document_id => "%{offenderId}"
            action => "index"
            validate_after_inactivity => 0
        }
    }
}