apiVersion: batch/v1
kind: CronJob
metadata:
  name: index-alias-management
spec:
  schedule: {{ .Values.management.schedule }}
  concurrencyPolicy: Forbid
  failedJobsHistoryLimit: 3
  successfulJobsHistoryLimit: 1
  jobTemplate:
    spec:
      template:
        spec:
          containers:
            - name: housekeeping
              image: ghcr.io/ministryofjustice/hmpps-probation-integration-services/person-search-index-from-delius-management:{{ .Values.version }}
              args:
                - /scripts/index-alias-management.sh
              env:
                - name: PRIMARY_INDEX_NAME
                  value: person-search-primary
                - name: STANDBY_INDEX_NAME
                  value: person-search-standby
                - name: SEARCH_INDEX_HOST
                  value: {{ .Values.management.host }}
                - name: APPLICATIONINSIGHTS_CONNECTION_STRING
                  valueFrom:
                    secretKeyRef:
                      name: common
                      key: APPLICATIONINSIGHTS_CONNECTION_STRING
                      optional: false
          restartPolicy: Never
