spring:
  activemq:
    broker-url: vm://localhost?broker.persistent=false&broker.useShutdownHook=false
  datasource:
    url: jdbc:h2:file:./test;MODE=Oracle;DEFAULT_NULL_ORDERING=HIGH;AUTO_SERVER=true;AUTO_SERVER_PORT=9092;DB_CLOSE_ON_EXIT=FALSE
  jpa:
    hibernate:
      ddl-auto: create-drop

oauth:
  endpoint:
    url: http://localhost:8500/auth
  client:
    id: prison-case-notes-to-probation
    secret: prison-case-notes-to-probation

integrations:
  nomis:
    url: http://localhost:8500

logging:
  level:
    org.hibernate.tool.schema: ERROR